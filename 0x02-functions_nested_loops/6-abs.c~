/**
 * _abs - Computes the absolute value of an integer
 *@int: number being tested
 * Return: Always 0.
 */

int _abs(int)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
