#include "holberton.h"
#include <stdio.h>

/**
 * _strlen - returns the length of the string
 *@s: string being counted
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int i;

	while (str[i] != 0)
		i++;
	_putchar(i);
}
