#include "holberton.h"

/**
 * _puts_recursion - prints a string
 * @s: characters being printed
 *
 */

void _puts_recursion(char *s)
{
	if (*s == '\0')
	{
		return;
	_putchar(*s);
	_putchar(++s);
	}
	_putchar('\n');
}
